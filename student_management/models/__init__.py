# this file makes python treat the folder as package
# Import models package so Odoo can find our model classes


# Import all model files so Odoo can register the models
from . import student
from . import course
